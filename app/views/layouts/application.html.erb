<!DOCTYPE html>
<html>
<head>
  <title>Wedding</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
	<%= stylesheet_link_tag "styles", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header class="main-header">
		<hgroup>
			<h2 class="name">Kate</h2>
			<h1 class="wedding">Wedding</h1>
			<h2 class="name">Stuart</h2>
		</hgroup>
		<nav class="main-nav">
			<ul>
				<%= link_to "When", root_path(anchor: "when") %>
				<%= link_to "Where", root_path(anchor: "where") %>
				<%= link_to "How", root_path(anchor: "how") %>
				<%= link_to "Registry", root_path(anchor: "registry") %>
				<%= link_to "RSVP", root_path %>
			</ul>
		</nav>
	</header>
	
<%= yield %>

</body>
</html>
